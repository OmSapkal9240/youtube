#include <stdio.h>

int main() {
    float c = 37.0;
    float f;  // Declare the variable 'f' before using it

    f = (9.0 / 5.0) * c + 32;  // Use floating-point division
    printf("The value in Fahrenheit is %f\n", f);

    return 0;
}
