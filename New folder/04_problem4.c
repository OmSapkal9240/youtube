#include <stdio.h>

int main() {
    float p = 34.1; // Change int to float for decimal values
    int r = 8;
    int t = 5;
    printf("The value of simple interest is %f\n", (p * r * t) / 100); // Fixed the formula
    return 0;
}
