#include <stdio.h>
int main()
{
    int i = 10; // Declare and initialize 'i' with 10
    int j = i;
    int a = 2, b = 3, c =  5;
    // %d is called formed specifier
    // %d is for int, %f is for float and %c is for char
    printf("the value of i is %d and value of j is %d", i, j );
    return 0;
}